# dependencies
node_modules/

# build output
.next/
out/
dist/
coverage/
.turbo/

# graphify — AST cache is reproducible; do not commit
graphify-out/cache/

# env
.env
.env.*
!.env.example
!.env.mirror.example

# system
.DS_Store
.vscode/
*.log
*.tsbuildinfo

# local DB dumps (never commit)
backups/

# deploy marker — written by scripts/deploy.sh, watched at runtime for live update detection
apps/web/.deployment-version

# sensitive data
*Educator*
*Heirarchy*
*.csv
