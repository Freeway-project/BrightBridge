# @coursebridge/workflow

Shared workflow roles, statuses, transition rules, and audit-friendly workflow helpers will live here.

Keep workflow status strings centralized in this package instead of hardcoding them across the app.
